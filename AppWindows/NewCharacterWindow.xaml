<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DnDApp.AppWindows.NewCharacterWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DnDApp.AppWindows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="NewCharacterWindow">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

	<Grid x:Name="BaseGrid" HorizontalAlignment="Center" VerticalAlignment="Center" Width="700" Height="500">
		<!--<StackPanel Width="400" Height="200">
			<Grid Margin="0 0 0 20">
				<Grid.ColumnDefinitions>
					<ColumnDefinition/>
					<ColumnDefinition/>
				</Grid.ColumnDefinitions>

				<TextBlock Text="Name:" Margin="0 6 40 0" Grid.Column="0"/>
				<TextBox PlaceholderText="Character name" Width="200" Grid.Column="1"/>
			</Grid>
			<Grid Margin="0 0 0 20">
				<Grid.ColumnDefinitions>
					<ColumnDefinition/>
					<ColumnDefinition/>
				</Grid.ColumnDefinitions>

				<TextBlock Text="Location:" Margin="0 6 40 0" Grid.Column="0"/>
				<Button Content="Browse" Width="200" Grid.Column="1"/>
			</Grid>
			<Grid Margin="0 0 0 20">
				<Grid.ColumnDefinitions>
					<ColumnDefinition/>
					<ColumnDefinition/>
				</Grid.ColumnDefinitions>

				<TextBlock Text="Accent Colour:" Margin="0 6 40 0" Grid.Column="0"/>
				<StackPanel Grid.Column="1">
					<TextBox PlaceholderText="Colour hex code" Width="200"/>
					<TextBlock Text="Optional" FontSize="10" Foreground="Gray" Margin="5 0 0 0"/>
				</StackPanel>
			</Grid>
		</StackPanel>

		<Grid HorizontalAlignment="Right" VerticalAlignment="Bottom">
			<Grid.ColumnDefinitions>
				<ColumnDefinition/>
				<ColumnDefinition/>
			</Grid.ColumnDefinitions>

			<Button Content="Cancel" Click="CancelButton_Click" Width="70" Margin="0 0 10 0" Grid.Column="0"/>
			<Button Content="Next" Click="NextButton_Click" Width="70" Grid.Column="1"/>
		</Grid>-->
	</Grid>
</Window>
