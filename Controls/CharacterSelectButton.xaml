<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DnDApp.Controls.CharacterSelectButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DnDApp.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Button Click="CharButton_Click" HorizontalAlignment="Stretch" Background="Transparent" BorderBrush="Transparent">
		<Grid Width="500">
			<StackPanel Orientation="Horizontal">
				<Grid Width="50" Margin="0 2 5 0" CornerRadius="100">
					<Image x:Name="CharImage" Source="null"/>
				</Grid>
				<StackPanel VerticalAlignment="Center">
					<TextBlock x:Name="NameText" Text="null" FontSize="16"/>
					<StackPanel Orientation="Horizontal"  Width="300">
						<TextBlock x:Name="ImagePathText" Visibility="Collapsed" Text="null" FontSize="12"/>
						<TextBlock x:Name="LevelText" Text="null" FontSize="12"/>
						<AppBarSeparator Height="20" Padding="4"/>
						<TextBlock x:Name="SpeciesText" Text="null" FontSize="12"/>
						<AppBarSeparator Height="20" Padding="4"/>
						<TextBlock x:Name="ClassText" Text="null" FontSize="12"/>
					</StackPanel>
				</StackPanel>
			</StackPanel>
			<DropDownButton Content="Options" Background="Transparent" BorderBrush="Transparent" HorizontalAlignment="Right" Margin="0 0 28 0">
				<DropDownButton.Flyout>
					<MenuFlyout Placement="Bottom">
						<MenuFlyoutItem Text="Edit"/>
						<MenuFlyoutItem Text="Locate"/>
						<MenuFlyoutItem Text="Delete"/>
					</MenuFlyout>
				</DropDownButton.Flyout>
			</DropDownButton>
		</Grid>
	</Button>
</UserControl>
